{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "@parcel/transformer-typescript-tsc": "^2.8.0",
    "@parcel/validator-typescript": "^2.8.0",
    "parcel": "^2.8.0"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  },

  "scripts": {
    "start": "parcel src/*.html",
    "prebuild": "npx parcel build src/*.html --public-url ./",
    "deploy": "gh-pages -d dist"
}
}
